# HTTPS example — copy to ssl.conf and adjust after obtaining certificates.
#
# Option A: certbot with webroot
#   certbot certonly --webroot -w /var/www/certbot -d archive.example.com
#
# Option B: place fullchain.pem and privkey.pem in deploy/nginx/certs/

# server {
#     listen 443 ssl;
#     server_name archive.example.com;
#
#     ssl_certificate     /etc/nginx/certs/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/privkey.pem;
#
#     location = / {
#         return 302 /archive/documents;
#     }
#
#     location = /health {
#         proxy_pass http://archive_api/health;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location = /archive {
#         return 302 /archive/;
#     }
#
#     location /archive/ {
#         proxy_pass http://archive_api/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_set_header X-Forwarded-Prefix /archive;
#     }
# }
